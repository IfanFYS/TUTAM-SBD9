{
  "name": "tutam9_todo_app",
  "version": "1.0.0",
  "description": "Todo App with React frontend and Express backend",
  "scripts": {
    "start:backend": "cd tutam9_backend && npm start",
    "start:frontend": "cd tutam9_frontend && npm run dev",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "npm install && cd tutam9_backend && npm install && cd ../tutam9_frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
